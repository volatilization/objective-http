module.exports = {
    server: require('./server'),
    client: require('./client'),
};

// http request as envent

const endpoint1 = {
    listenwhat = 'get:/hello'

    process(request) {
        return request
    }
}

const event1 = {
    inpursteam;
}

const endpoint1_1 = {
    trigger = event1

    do() {
        return event2
    }
}

const event2 = {
    outputstream
}
